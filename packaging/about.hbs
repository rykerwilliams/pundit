pundit: licences of the Rust crates compiled into /usr/bin/pundit

Generated by cargo-about from Cargo.lock. The C and C++ libraries linked
into the binary, and the embedded fonts, are covered in
/usr/share/doc/pundit/copyright.

Overview
--------
{{#each overview}}
{{{name}}} ({{count}})
{{/each}}
{{#each licenses}}

================================================================================
{{{name}}} ({{id}})
Used by:
{{#each used_by}}
  {{crate.name}} {{crate.version}}
{{/each}}
--------------------------------------------------------------------------------
{{{text}}}
{{/each}}
